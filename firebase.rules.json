{
  "rules": {
    ".read": true,
    ".write": "auth !== null",
    "students": { ".indexOn": ["studentId", "email"] },
    "enrollments": { ".indexOn": ["studentId"] },
    "announcements": { ".read": true, ".write": "auth !== null" }
  }
}
